import styled from 'styled-components';
import { Button, Form } from 'antd';

const Amount = styled.div`
  text-align: center;
  width: 100%;
  margin-top: 20px;
  & span {
    font-size: 20px;
    &:first-child {
      font-size: 60px;
    };
  }
`;

export default {
  Amount
};